const entrada = require("readline-sync")

const real = entrada.questionInt("Informe a produção real:");
const prevista = entrada.questionInt("Informe a produçõa prevista: ");


function calcularEficiencia(real, prevista)  {
    const
}

function classificarEficiencia(percentual) {

}