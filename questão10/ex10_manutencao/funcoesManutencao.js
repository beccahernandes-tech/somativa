function calcularMaoDeObra(horas) {
    hora = 80;
}

function calcularTotal(valorPecas, horas) {
    require = pecas + hora 
}

function verificarGarantia(meses) {
    if (meses <= 6) {
        console.log("EM GARANTIA");
    } else {
        console.log("FORA DA GARANTIA ");
    }
}

module.exports {
    calcularMaoDeObra,
    calcularTotal,
    verificarGarantia
}